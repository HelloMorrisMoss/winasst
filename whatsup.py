import tkinter as tk
import sqlite3

root = tk.Tk()
# canv1 = tk.Canvas(root, width=400, height=300)
# in_box = tk.Entry(root)
# canv1.create_window(200, 140, window=in_box)

def

b1 = tk.Button(root, text="words")
b1.pack()

in_box = tk.Entry(root)
in_box.pack()

root.mainloop()